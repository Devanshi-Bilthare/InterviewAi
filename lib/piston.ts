/**
 * Piston public API is whitelist-only as of Feb 2026.
 * Use lib/judge0.ts (free Judge0 CE at https://ce.judge0.com) instead.
 */
export {
  runCode,
  executeWithTestCases,
  type ExecutionResult,
} from "./judge0";
